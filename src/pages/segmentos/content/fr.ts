// ─────────────────────────────────────────────────────────────────────────────
// Conteúdo fr (Français) de segmentos.
//
// FASE 2 — FALLBACK TEMPORARIO: reexporta o conteúdo pt-br enquanto as
// traduções não forem revisadas pelo CEO (Fase 4+). Quando as traduções
// estiverem prontas, substituir este arquivo pelo conteúdo real em francês.
// ─────────────────────────────────────────────────────────────────────────────

import ptBr from './pt-br';
import type { SegmentContentMap } from './types';

// TODO (Fase 4): substituir pelo conteúdo real em francês revisado pelo CEO.
const fr: SegmentContentMap = ptBr;

export default fr;
