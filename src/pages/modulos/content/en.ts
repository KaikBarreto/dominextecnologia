// ─────────────────────────────────────────────────────────────────────────────
// Conteúdo en (English) de módulos.
//
// FASE 2 — FALLBACK TEMPORÁRIO: reexporta o conteúdo pt-br enquanto as
// traduções não forem revisadas pelo CEO (Fase 4+). Quando as traduções
// estiverem prontas, substituir este arquivo pelo conteúdo real em inglês.
// ─────────────────────────────────────────────────────────────────────────────

import ptBr from './pt-br';
import type { ModuleContentMap } from './types';

// TODO (Fase 4): substituir pelo conteúdo real em inglês revisado pelo CEO.
const en: ModuleContentMap = ptBr;

export default en;
